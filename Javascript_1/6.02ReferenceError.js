let multipl = (a,b) => a + b;
let result = multiply (10,20);
console.log(result); // -> 30

//MISSING : "y" in multiply 

// The typo in the word console is a semantic error, in JavaScript called a ReferenceError